Instrukcja uruchomienia:  
```
java Battleship -mode [server|client] -port N -map plik -host hostName
```
na przykład:
```
java Battleship -mode server -port 8080 -map mapa1.txt
```
```
java Battleship -mode client -host localhost -port 8080 -map mapa2.txt
```
